apiVersion: v1
kind: ResourceQuota
metadata:
  name: infras-cli-quota
  namespace: infras-cli
  labels:
    app: infras-cli
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "512Mi"
    limits.cpu: "1000m"
    limits.memory: "1Gi"
    persistentvolumeclaims: "0"
